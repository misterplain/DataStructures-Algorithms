import React from 'react'
import { Grid, Typography, Box, Card, CardContent } from "@mui/material";

const Problems = () => {
  return (
    <Grid container sx={{ justifyContent: "center" }}>
    <Grid item xs={10}>
      <Typography variant='h4'>Practice Problems</Typography>
    </Grid>
  </Grid>
  )
}

export default Problems